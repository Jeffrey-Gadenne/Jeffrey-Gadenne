{
  "name": "device-diagnostics",
  "version": "1.0.0",
  "description": "AI-powered device diagnostics and donor-parts identification from photos and video",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.109.1",
    "express": "^5.2.1"
  }
}
